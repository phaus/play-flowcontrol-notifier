# Application dependencies

require:
    - play
    - local -> flowcontrol-notifier 0.1

repositories:
    - playCustomModules:
        type:       local
        artifact:   ${application.path}/../../dist/[module]-[revision].zip
        contains:
          - local -> *